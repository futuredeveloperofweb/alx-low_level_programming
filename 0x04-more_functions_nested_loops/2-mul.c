#include "main.h"

/*
 * mul - it takes two integers and multiply theme
 * @a: the first integer to muliply with
 * @b: second integer to multiply with
 * Return: the multipplied integer
 */

int mul(int a, int b)
{
	return (a * b);
}
